# PostgreSQL statement timeout 설정 예시
# Supabase를 사용하는 경우:

# 기존 DATABASE_URL에 타임아웃 파라미터 추가
# connect_timeout: 연결 시도 타임아웃 (초)
# statement_timeout: SQL 문 실행 타임아웃 (밀리초)
# pool_timeout: 커넥션 풀에서 연결을 기다리는 최대 시간 (초)

DATABASE_URL="postgresql://user:password@host:6543/db?pgbouncer=true&connect_timeout=30&pool_timeout=30&statement_timeout=30000"
DIRECT_URL="postgresql://user:password@host:5432/db?connect_timeout=30&statement_timeout=30000"

# 권장 설정:
# - connect_timeout=30 (30초)
# - pool_timeout=30 (30초)
# - statement_timeout=30000 (30초)

# 대량 데이터 처리 시:
# - statement_timeout=60000 (60초) 또는 더 길게 설정

# Prisma 연결 풀 설정 (선택사항)
# DATABASE_URL에 추가 가능한 파라미터:
# - connection_limit=10 (최대 연결 수)
# - pool_timeout=30 (풀 타임아웃)